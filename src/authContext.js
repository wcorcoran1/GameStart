/**
 * A context that holds the authentication state of the user.
 * returns {object} - The context object.
 */
import { createContext } from "react";

const AuthContext = createContext({});

export default AuthContext;
